// Importing the CLI class
const CLI = require('./lib/cli.js');

// Creating an instance of the CLI class
const cli = new CLI();

// Displaying a welcome message
console.log('Welcome to SVG Logo Maker! 🙂');

// Running the CLI application
cli.run();
